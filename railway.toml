[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python app.py"
healthcheckPath = "/api/status"
healthcheckTimeout = 300

[variables]
PORT = "5000"
PYTHON_VERSION = "3.11"
FLASK_ENV = "production"
```

---

## 📁 **FILE 5: `Procfile` (Heroku Backup)**
```
web: gunicorn app:app
